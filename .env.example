# Copy to .env.local for development. Set these in Dokploy for production.

# Canonical origin — drives absolute URLs in metadata and Open Graph tags.
# Optional in development; required for correct canonical links in production.
NEXT_PUBLIC_SITE_URL=https://playground.sonomusa.com
