/**
 * Placeholder home route.
 *
 * The gallery arrives in Phase 5. This exists so the toolchain has something
 * to build against while the domain contract is being established.
 */
export default function HomePage() {
  return (
    <main className="flex min-h-dvh items-center justify-center p-8">
      <p className="font-mono text-xs uppercase tracking-[0.18em] text-(--color-text-secondary)">
        SonoMusa Playground
      </p>
    </main>
  )
}
